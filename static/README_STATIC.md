# Static deploy (Vercel)

Le site est généré via `static_site.html` sans build Node.

## Déploiement Vercel
- Dans Vercel, crée un projet **Static** (aucune commande de build)
- Pointer le dossier vers `static/`
- Fichiers à servir : `static_site.html`, `styles.css`, `profile.jpg` (optionnel)

